<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {% for page in pages %}
    <url>
      <loc>{{ page.url }}</loc>
      <lastmod>{{ now.isoformat(timespec='seconds') }}</lastmod>
      <changefreq>{% if page.rel_path.stem == "index" %}daily{% else %}monthly{% endif %}</changefreq>
    </url>
  {% endfor %}
</urlset>
